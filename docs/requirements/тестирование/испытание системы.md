# Программа и методика испытаний RRDCS

## 1. Титульная часть
- Наименование: `RRDCS (Repository Rules for Code Safety)`.
- Вид испытаний: приемочные испытания качества PR-gates и инструментального контура.
- Версия/дата: `1.0 / 2026-04-03`.
- Нормативные ссылки (внутренний стандарт):
  - `docs/requirements/требования/требования_внутренний_стандарт.md`
  - `docs/requirements/обоснование выбора.md`
  - `docs/requirements/структура ПО/стек проекта.md`
  - `docs/requirements/архитектура/описание AL.md`
  - `docs/requirements/архитектура/описание TL.md`
  - `docs/requirements/структура Данных/описание Данных.md`

## 2. Общие положения
- Назначение: подтвердить, что `merge` в защищаемую ветку разрешается только при прохождении обязательных quality gates.
- Объект испытаний:
  - в области охвата (in scope):
    - Оркестрация по событию pull request (PR-triggered orchestration).
    - Обязательные проверки (Required checks): lint/static, security, matrix, evidence publication.
    - Решение о merge (Merge decision) и публикация доказательной базы.
  - вне области охвата (out of scope):
    - прикладная бизнес-функциональность внешних систем;
    - выделенная прикладная БД (на MVP не используется).
- Основания:
  - FR/NFR и тест-сценарии `T-001 ... T-011` из `требования_внутренний_стандарт.md`, раздел `8.3`;
  - модель слоев и сервисов из `описание AL.md` и `описание TL.md`.

## 3. Приемочные критерии

| Идентификатор требования | Текст требования | Значение/порог | Источник |
|---|---|---|---|
| AC-001 | Обязательные проверки запускаются на каждом PR | факт запуска обязательных jobs (pass/fail) | FR-001 |
| AC-002 | Merge блокируется при fail required check | merge недоступен при `failed` | FR-002 |
| AC-003 | Merge разрешается при pass required checks | merge доступен при `pass` | FR-003 |
| AC-004 | Security baseline выполняется на каждом PR | есть security jobs на каждом PR | FR-007, NFR-003 |
| AC-005 | Для fail-check доступны причина и лог | причина + `log_ref` для каждого fail | FR-008, NFR-006 |
| AC-006 | Кросс-платформенность core checks | минимум `windows` и `linux` matrix | FR-009 |
| AC-007 | Core pipeline производительности | median `<= 15 минут` | NFR-002 |
| AC-008 | Runtime/toolchain зафиксированы | нет плавающих major | NFR-004 |
| AC-009 | Coverage режим MVP | coverage собирается non-blocking | NFR-008 |
| AC-010 | Onboarding нового репозитория | подключение через профиль и переход `audit -> required` без изменения core scripts | FR-011, FR-013, NFR-009 |

## 4. Условия проведения испытаний
- Стенд:
  - `GitHub Linux Runner` — исполнение Linux jobs.
  - `GitHub Windows Runner` — исполнение Windows jobs.
  - `Developer Workstation` — локальный pre-check (`check-all`).
- ПО и конфигурации:
  - `Python 3.12 Runtime`.
  - `PowerShell 7+`, `Bash 5+`.
  - Среда выполнения GitHub Actions (GitHub Actions Runtime), `GitHub Checks API`, `GitHub Artifacts`, `GitHub Cache`.
- Сетевые условия:
  - для документационных рендеров используется локальный `Kroki` (`localhost:${KROKI_PORT:-8000}`).
- Контроль работоспособности:
  - доступность логов job workflow и PR summary;
  - доступность ссылок на артефакты (artifact links) для результатов проверок.

## 5. Подготовка к испытаниям
1. Подтвердить включенные branch protection и required checks.
2. Подтвердить наличие policy/manifests в репозитории как source of truth.
3. Подтвердить доступность CI раннеров для Windows и Linux.
4. Подтвердить, что диагностика fail-check публикуется в summary/artifacts.
5. Подтвердить единый локальный pre-check сценарий (`check-all`) для паритета с CI core checks.

## 6. Реестр проверок

| Идентификатор проверки | Наименование | Источник (docs + раздел) | Проверяемый параметр | Вид испытаний | Условия/стенд | Артефакты | Критерий pass/fail |
|---|---|---|---|---|---|---|---|
| T-001 | Блокировка merge при падении lint check | `требования_внутренний_стандарт.md`, `8.3` | обязательные lint/static checks | негативный | PR в GitHub Actions | lint logs, PR checks status | успех: merge blocked; неуспех: merge allowed |
| T-002 | Разрешение merge при полном pass required checks | `требования_внутренний_стандарт.md`, `8.3` | итог обязательных проверок | позитивный | PR в GitHub Actions | PR checks status, summary | успех: merge available; неуспех: merge blocked |
| T-003 | Паритет local pre-check и CI core checks | `требования_внутренний_стандарт.md`, `8.3` | эквивалентность core-check набора | паритет | локально + PR CI | local log, CI log, compare note | pass: результаты эквивалентны; fail: рассинхрон |
| T-004 | Срабатывание baseline security checks | `требования_внутренний_стандарт.md`, `8.3` | обнаружение секретов / SAST (secret/SAST detection) | security негативный | PR в GitHub Actions | security logs/findings | успех: security fail блокирует merge; неуспех: merge доступен |
| T-005 | Наблюдаемость fail-сценариев | `требования_внутренний_стандарт.md`, `8.3` | полнота диагностических доказательств (diagnostic evidence) | наблюдаемость | PR в GitHub Actions | summary + artifact links | успех: причина и ссылка на лог есть по каждому fail |
| T-006 | Кросс-платформенный запуск | `требования_внутренний_стандарт.md`, `8.3` | выполнение проверок в matrix | платформенный | Windows+Linux runners | matrix logs | pass: jobs success на обеих ОС |
| T-007 | Подключение нового check без переписывания ядра | `требования_внутренний_стандарт.md`, `8.3` | расширяемость оркестрации (extensibility of orchestration) | расширяемость | PR в GitHub Actions | diff + execution logs | pass: новый check включен без изменения core flow |
| T-008 | Производительность core pipeline | `требования_внутренний_стандарт.md`, `8.3` | медианная длительность (median duration) | аналитический | выборка PR runs | duration report | pass: median <= 15 мин |
| T-009 | Фиксация версий runtime/toolchain | `требования_внутренний_стандарт.md`, `8.3` | политика закрепления версий (pinning policy) | аудит | workflows/config review | pin-audit report | pass: версии зафиксированы |
| T-010 | Coverage на MVP | `требования_внутренний_стандарт.md`, `8.3` | режим сбора coverage (coverage collection mode) | аналитический | CI coverage job | coverage report | успех: coverage есть и non-blocking |
| T-011 | Onboarding нового репозитория и rollout режима | `требования_внутренний_стандарт.md`, `8.3` | подключение репозитория и переход `audit -> required` (repository onboarding) | mixed | governance + PR CI | onboarding checklist, audit/required run logs | успех: onboarding выполнен без изменения core scripts |

## 7. Методики испытаний (по ID проверок)

### T-001 — Блокировка merge при падении lint check (lint/static)
- Цель: подтвердить обязательность lint/static quality gate.
- Основание: FR-001, FR-002, FR-006; NFR-001.
- Средства: PR workflow required checks.
- Шаги:
  1. Создать PR с нарушением lint/static.
  2. Дождаться завершения required jobs.
  3. Проверить статус merge.
- Ожидаемый результат: merge заблокирован.
- Артефакты: job logs, checks status.
- Оценка соответствия: pass/fail.

### T-002 — Разрешение merge при полном pass required checks (required checks)
- Цель: подтвердить корректный позитивный исход gate-контроля.
- Основание: FR-003; NFR-001.
- Средства: PR workflow required checks.
- Шаги:
  1. Исправить нарушения в PR.
  2. Повторно выполнить required checks.
  3. Проверить доступность merge.
- Ожидаемый результат: merge разрешен.
- Артефакты: checks status, PR summary.
- Оценка соответствия: pass/fail.

### T-003 — Паритет local pre-check и CI core checks (core checks)
- Цель: подтвердить отсутствие дрейфа локального и CI наборов.
- Основание: FR-004; NFR-005.
- Средства: `check-all` + CI core jobs.
- Шаги:
  1. Запустить локальный `check-all`.
  2. Открыть/обновить PR.
  3. Сравнить результат core checks.
- Ожидаемый результат: статусы эквивалентны.
- Артефакты: local log, CI log, compare report.
- Оценка соответствия: pass/fail.

### T-004 — Срабатывание baseline security checks (security baseline)
- Цель: подтвердить блокировку при security нарушениях.
- Основание: FR-007; NFR-003.
- Средства: security jobs (`gitleaks`, `CodeQL` профиль).
- Шаги:
  1. Подготовить тестовое security-нарушение.
  2. Запустить PR checks.
  3. Проверить merge status.
- Ожидаемый результат: merge blocked.
- Артефакты: findings report, security logs.
- Оценка соответствия: pass/fail.

### T-005 — Наблюдаемость fail-сценариев (observability)
- Цель: подтвердить полноту доказательной базы.
- Основание: FR-008; NFR-006.
- Средства: PR summary + GitHub Artifacts.
- Шаги:
  1. Инициировать контролируемый fail одного required check.
  2. Проверить summary и ссылку на logs/artifacts.
  3. Проверить описание причины fail.
- Ожидаемый результат: причина и `log_ref` присутствуют.
- Артефакты: run summary, artifact links.
- Оценка соответствия: pass/fail.

### T-006 — Кросс-платформенный запуск (Windows/Linux)
- Цель: подтвердить выполнение core checks на Windows и Linux.
- Основание: FR-009.
- Средства: matrix CI jobs.
- Шаги:
  1. Запустить pipeline с матрицей `windows+linux`.
  2. Проверить исходы jobs.
- Ожидаемый результат: успех в обеих ОС.
- Артефакты: matrix logs.
- Оценка соответствия: pass/fail.

### T-007 — Подключение нового check без переписывания ядра (extensibility)
- Цель: подтвердить расширяемость orchestration.
- Основание: FR-010; NFR-007.
- Средства: конфигурационное подключение check.
- Шаги:
  1. Добавить новый check в конфигурационный слой.
  2. Запустить PR workflow.
  3. Проверить выполнение нового check и отсутствие изменений core flow.
- Ожидаемый результат: новый check выполняется, core orchestration не переписан.
- Артефакты: change diff, workflow logs.
- Оценка соответствия: pass/fail.

### T-008 — Производительность core pipeline (performance)
- Цель: подтвердить целевое медианное время.
- Основание: NFR-002.
- Средства: статистика длительностей workflow.
- Шаги:
  1. Собрать выборку по N PR.
  2. Рассчитать median.
  3. Сравнить с целевым порогом.
- Ожидаемый результат: median `<= 15 минут`.
- Артефакты: performance summary.
- Оценка соответствия: pass/fail.

### T-009 — Фиксация версий runtime/toolchain (pinning)
- Цель: подтвердить воспроизводимость toolchain.
- Основание: NFR-004, BR-PG-02.
- Средства: audit workflows/config/manifests.
- Шаги:
  1. Проверить runtime/toolchain versions.
  2. Проверить отсутствие плавающих major.
- Ожидаемый результат: версии закреплены.
- Артефакты: pin-audit report.
- Оценка соответствия: pass/fail.

### T-010 — Coverage на MVP (non-blocking)
- Цель: подтвердить режим сбора coverage как метрики.
- Основание: NFR-008.
- Средства: coverage job.
- Шаги:
  1. Запустить coverage job в CI.
  2. Проверить, что результат публикуется.
  3. Проверить, что job informational и не блокирует merge.
- Ожидаемый результат: coverage отчет есть, merge не блокируется этим job.
- Артефакты: coverage report.
- Оценка соответствия: pass/fail.

### T-011 — Onboarding нового репозитория и rollout режима (repository onboarding)
- Цель: подтвердить простое подключение репозитория к RRDCS и управляемый переход `audit -> required`.
- Основание: FR-011, FR-013, NFR-009.
- Средства: Repository Integration Profile + reusable workflow.
- Шаги:
  1. Создать профиль интеграции репозитория в режиме `audit`.
  2. Запустить PR checks и подтвердить публикацию статусов/summary.
  3. Перевести профиль в режим `required`.
  4. Повторно запустить PR checks и подтвердить блокировку merge при fail required checks.
- Ожидаемый результат: onboarding и переход режима выполнены без изменения core scripts.
- Артефакты: onboarding checklist, governance decision log, run logs (`audit`/`required`).
- Оценка соответствия: pass/fail.

## 8. Порядок проведения испытаний
1. Проверка готовности условий (раздел 5).
2. Выполнение обязательных негативных/позитивных gate проверок `T-001`, `T-002`, `T-004`.
3. Выполнение наблюдаемости и кросс-платформенности `T-005`, `T-006`.
4. Выполнение паритета и расширяемости `T-003`, `T-007`.
5. Выполнение аналитических и onboarding-проверок `T-008`, `T-009`, `T-010`, `T-011`.
6. Формирование итогового протокола.

Переход между шагами разрешен только после фиксации результатов предыдущего шага.

## 9. Протоколирование и отчетность

### 9.1 Поля протокола
- `ID проверки`.
- `Результат`.
- `Измерения/значения`.
- `Замечания`.
- `Ссылки на артефакты`.

### 9.2 Таблица протокола

| Идентификатор проверки | Результат | Измерения/значения | Замечания | Ссылки на артефакты |
|---|---|---|---|---|
| T-001 | `pass/fail` | статус required checks | причина блокировки/разблокировки | links |
| T-002 | `pass/fail` | статус required checks | — | links |
| T-003 | `pass/fail` | эквивалентность статусов | рассинхроны при наличии | links |
| T-004 | `pass/fail` | security status | уровень findings | links |
| T-005 | `pass/fail` | полнота summary | пропуски reason/log_ref | links |
| T-006 | `pass/fail` | статус windows/linux | проблемы, зависящие от платформы (platform-specific issues) | links |
| T-007 | `pass/fail` | факт подключения check | влияние на core flow | links |
| T-008 | `pass/fail` | медианная длительность (median duration) | расчетный период | links |
| T-009 | `pass/fail` | pinning status | найденные drift-факты | links |
| T-010 | `pass/fail` | coverage report published | blocking/non-blocking mode | links |
| T-011 | `pass/fail` | onboarding checklist + режим enforcement | отклонения по rollout | links |

### 9.3 Формат evidence (A3-04 baseline)
- Для каждого запуска `check-all` публикуются:
  - `check-all.log` — полный stdout/stderr запуска;
  - `check-all-report.json` — машинный отчет (`overall_status`, `failed_checks`, `error_code`, `source_step`);
  - `check-all-summary.md` — читаемое summary для PR.
- Для решения о gate (gate-decision) публикуются:
  - `gate-report.json` — итог по linux/windows jobs;
  - `gate-summary.md` — итоговая таблица статусов и ссылочных `log_ref`.
- Обязательное правило T-005: при `fail` в отчете должны присутствовать `error_code`, `source_step` и `log_ref`.

### 9.4 Операционные сигналы stage-2
- Этот раздел используется для следующего этапа развития и не меняет базовый MVP-контур.
- Для stage-2 публикуются только сигналы, которые влияют на решение о переводе `audit-only -> pilot blocking -> global blocking`.
- Минимальный набор сигналов:
  - медианное время core pipeline;
  - доля прогонов с полным пакетом evidence;
  - доля ложных блокировок, снятых после повторного прогона;
  - время возврата в `audit` после инцидента;
  - количество ручных вмешательств в pipeline.
- Формат публикации:
  - `stage-2-signals.json` — машинный отчет с полями `metric_id`, `period`, `value`, `threshold`, `status`, `artifact_links`;
  - `stage-2-summary.md` — читаемое сводное представление для review и release/change approval.
- Для каждого сигнала с `status=fail` обязательны:
  - `reason`;
  - `log_ref`;
  - ссылка на артефакт;
  - ссылка на `rollback_ref`, если инцидент потребовал отката.
- Сводка stage-2 должна быть связана с `docs/release/план-следующего-этапа.md` и `docs/operations/базовый-операционный-контур-гитхаб.md`, чтобы решения о переводе режима не принимались вне документированного контура.

## 10. Критерии завершения испытаний
- Все обязательные проверки `T-001 ... T-011` выполнены и протоколированы.
- Все критерии `AC-001 ... AC-010` подтверждены.
- Для всех `failed` случаев есть причина и ссылки на evidence.
- Нет блокирующих отклонений, противоречащих FR/NFR `MUST`.

## 11. Работы после завершения испытаний
- Сохранить итоговый протокол и ссылки на artifacts в репозитории.
- Зафиксировать отклонения и корректирующие действия (при наличии).
- Обновить baseline test stack и ПМИ при изменениях policy/toolchain/check-наборов.

## Связанные файлы
- `docs/requirements/тестирование/стек тестов.md`
- `docs/requirements/тестирование/маппинг_PMI_на_наборы.md`
- `docs/requirements/тестирование/маппинг.json`
- `docs/requirements/тестирование/реестр_проверок.json`
- `tests/acceptance/harness.py`
- `.github/workflows/rrdcs-test-harness.yml`
- `docs/requirements/тестирование/verification-report-2026-04-06.md`
- `docs/requirements/тестирование/coverage-roadmap.md`
- `docs/release/план-следующего-этапа.md`
- `docs/operations/базовый-операционный-контур-гитхаб.md`

## 12. Тестовый harness приемки T-001...T-011
- Локальный запуск:
- `python tests/acceptance/harness.py --output-json .artifacts/test-harness/local/harness-report.json --output-md .artifacts/test-harness/local/harness-report.md`
- CI запуск:
- workflow `.github/workflows/rrdcs-test-harness.yml`
- Назначение:
- единая точка контроля полноты покрытия `T-001...T-011`;
- автоматическая фиксация локально проверяемых пунктов (`T-003`, `T-006`, `T-007`, `T-009`, `T-010`, `T-011`);
- явная маркировка пунктов, требующих CI/PR evidence (`manual-required`).
- Артефакты:
- `.artifacts/test-harness/**`
- `rrdcs-test-harness-linux-evidence`
- `rrdcs-test-harness-windows-evidence`
- `rrdcs-coverage-evidence`
